* 想要在类以外使用枚举量之类的常量，必须声明成公有
* 默认参数一定要设置在函数声明中，设置在定义里会报错
* 对于类的内联函数inline关键字只要加在函数定义就可以了，不允许分离编译
* 对于成员函数const放在函数外面表示函数不会修改调用他的对象'Vector operator + (const Vector &)const;'
* 函数模板是个大坑，这东西不允许分离编译，声明和定义必须放一起，更具体的细节等我看到后面再写
* c++内置数学函数只支持弧度制，要想使用角度必须要转换
* 对于友元函数，'friend'关键字只要放在函数声明的位置就可以了
* %运算符的第二个操作数必须是整形变量
* 